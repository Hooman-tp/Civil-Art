/**
 * بافت ظریف «کاغذ نقشه‌کشی مهندسی» (blueprint grid) — امضای بصری
 * مشترکِ بخش‌های تیره‌ی سایت: خطوط طلاییِ خیلی کم‌رنگ که به هویت
 * «مهندسی» شرکت اشاره می‌کند، بدون رقابت با محتوا.
 *
 * قبلاً این ثابت مستقیم داخل app/page.tsx تعریف شده بود. چون حالا
 * ProcessTimeline.tsx هم به همین بافت نیاز دارد، به یک فایل مشترک
 * منتقل شد تا در چند کامپوننت بدون تکرارِ کد استفاده شود (DRY) —
 * هر کامپوننتی که در آینده به این بافت نیاز داشته باشد هم از همین‌جا
 * ایمپورت می‌کند.
 */
export const BLUEPRINT_GRID = `url("data:image/svg+xml,${encodeURIComponent(
  '<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48"><path d="M48 0.5H0V48" fill="none" stroke="rgba(212,175,55,0.07)" stroke-width="1"/></svg>'
)}")`;
